<div>
    <a href="{{ product.url }}">
        {{ product.featured_image | image_url: width: 400 |  image_tag }}
        <h3>{{- product.title }}</h3>
        <p>{{ product.price |  money }}</p>
    </a>
</div>
